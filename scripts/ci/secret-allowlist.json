{
  "_comment": "Controlled allowlist of KNOWN test-fixture secret VALUES only. This is per-value, not a path bypass. Do not add real secrets. Do not allowlist by path.",
  "allowedValues": [
    "test-signing-secret",
    "test-secret"
  ]
}
